services:
  - type: pserv
    name: anas-supplements-db
    plan: free
    postgresVersions:
      - 15

  - type: web
    name: ecommerce-backend
    plan: free
    rootDir: server
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: DATABASE_URL
        fromDatabase:
          name: anas-supplements-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://ecommerce-phi-five-35.vercel.app
    buildCommand: npm install && npx prisma generate && npm run build
    startCommand: npm start
    autoDeploy: true
